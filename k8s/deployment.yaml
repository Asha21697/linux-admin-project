apiVersion: apps/v1
kind: Deployment

metadata:
  name: technova

spec:
  replicas: 3

  selector:
    matchLabels:
      app: technova

  template:
    metadata:
      labels:
        app: technova

    spec:
      containers:
      - name: technova
        image: technova-cicd:latest
        imagePullPolicy: Never

        ports:
        - containerPort: 80
